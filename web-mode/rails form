# -*- mode: snippet; require-final-newline: nil -*-
# name: rails form
# key: form
# binding: direct-keybinding
# condition: (string= (td/current-buffer-extension) "erb")
# --
<%= form_tag(${1:url}_path, method: :${2:post}) do %>
  <%= submit_tag("${3:Go}", class: "") %>
<% end %>